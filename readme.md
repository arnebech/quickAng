### quickAng - Chrome DevTools Extension

Simple aliasing when doing angularJS dev.

Runs

```js
//...
$s = angular.element($0).scope();
$is = angular.element($0).isolateScope();
//...
```

when an element is selected in the dev tools or dev tools is opened.